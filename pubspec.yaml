name: muno_inventory
description: Muno Inventory - offline-first mobile inventory counting app.
publish_to: "none"
version: 0.1.0+1

environment:
  sdk: ">=3.5.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  crypto: ^3.0.6
  csv: ^6.0.0
  excel: ^4.0.6
  file_picker: ^13.1.0
  mobile_scanner: ^6.0.7
  path: ^1.9.1
  path_provider: ^2.1.5
  share_plus: ^13.3.0
  sqflite: ^2.4.2

dev_dependencies:
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
